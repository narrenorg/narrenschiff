files_path: "examples/files"
targetPort: 5000
# targetPort: 80  -> Two same values in one file will override

init_namespaces:
  - development
  - qa
  - staging
  - production

app_envs:
  APP_DEBUG: "true"  # Booleans must be quoted!!!
  APP_PORT: 5000
  APP_HOSTS: '*'
